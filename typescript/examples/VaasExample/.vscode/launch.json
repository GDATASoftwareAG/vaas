{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "envFile": "${workspaceFolder}/.env",
            "name": "Run Script: start",
            "request": "launch",
            "command": "npm run start",
            "cwd": "${workspaceFolder}"
        }
    ]
}